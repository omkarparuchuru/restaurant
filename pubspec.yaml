name: resto_admin
description: Restaurant admin app to accept/ready/hand over orders and track delivery.
publish_to: "none"

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  material_design_icons_flutter: ^7.0.7296
  cupertino_icons: ^1.0.6
  uuid: ^4.5.1
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/profile_placeholder.png
